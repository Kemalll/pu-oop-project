package Map.Castles;

import Map.Castle;
import Map.GameTile;

import java.awt.*;

public class CastleWest extends Castle {
    public CastleWest(int row, int col, boolean isSpawn) {
        super(row, col, isSpawn);

        for (int i = 0; i <=row; i++) {
            for (col = 0; col < 9; col++) {


            }
        }

    }
}

